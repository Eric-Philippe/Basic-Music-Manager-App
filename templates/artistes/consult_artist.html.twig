{% extends 'base_disco.html.twig' %}

{% block title %} Disco Time {% endblock %}

{% block body %}

<section class="vh-100">
  <div class="container py-2 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col col-md-9 col-lg-7">
        <div class="card" style="border-radius: 15px; background-color: #93e2bb;">
          <div class="card-body p-4 text-black">
            <div>
              <h4 class="mb-4">{{ artiste.getNomComplet() }}</h4>
              <div class="d-flex align-items-center justify-content-between mb-3">
                <p class="small mb-0">{{ artiste.getDateNaissance()|date('d-m-Y') }} | <i>{{ age }} ans</i> <br> {{ artiste.getLieuNaissance() }}</p>
                <p class="fw-bold mb-0"><strong>Rôle : </strong> {{ artiste.getType().getType() }}</p>
              </div>
            </div>
            <div class="d-flex align-items-center mb-4">
              <div class="flex-shrink-0">
                <img src={{ artiste.getPhoto() }}
                  alt="Generic placeholder image" class="img-fluid rounded-circle border border-dark border-3"
                  style="width: 70px;">
              </div>
              <div class="flex-grow-1 ms-3">
                <div class="d-flex flex-row align-items-center mb-2">
                  <p class="mb-0 me-2">{{ artiste.getAt() }}</p>
                </div>
                <div>
                  <button type="button" class="btn btn-outline-dark btn-rounded btn-sm"
                    data-mdb-ripple-color="dark"><a class="hidden-link" href="https://www.instagram.com/accounts/login/"> + Follow </a></button>
                 
                    {% for song in songFromArtiste %}
                    <button type="button" class="btn btn-outline-dark btn-rounded btn-sm"
                    data-mdb-ripple-color="dark"><a class="hidden-link" href="{{ path('app_chanson_consult', {'slug': song.id}) }}">💽 {{ song.getTitre() }}</a></button>
                    {% endfor %}
                </div>
              </div>
            </div>
            <hr>
            <div class="d-flex justify-content-between align-items-center mb-2">
              <p class="small mb-0">💽 {{ songFromArtiste|length }} album(s) à son actif</p>
            </div>
            <hr>
            <p class="my-4 pb-1"><strong>Description :</strong> {{ artiste.getDescription() }}</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

{% endblock %}
